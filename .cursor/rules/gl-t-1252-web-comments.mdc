---
description: GL/T-1252 Web 前端注释规范（光力科技）
globs: "**/*.{vue,js,ts,jsx,tsx,css,scss,less,html}"
alwaysApply: false
---

# GL/T-1252 Web 注释规范

来源：光力科技《Web前端编码规范》第1版（2025-10-10）

## 原则

- 如无必要，勿增注释；如有必要，尽量详尽
- 注释内容与注释符之间保留空格；注释使用中文

## HTML

- 单行：`<!-- 描述 -->`，写在被注释代码上方单独一行
- 模块起止：`<!-- S 模块名 -->` … `<!-- E 模块名 -->`，模块间空一行
- 嵌套模块：注释写在结束标签下方单独一行，避免与主模块混淆

## CSS

- 单行：`/* 描述 */`，单独一行
- 模块：首行写模块信息，分隔线与 `*/` 另起一行
- 文件头注明：描述、作者、创建日期

## JavaScript / Vue

方法注释示例：

```javascript
/**
 * @description 方法描述
 * @param {Number} id 参数描述
 * @return {Object} 返回值描述
 */
```

- 单行用 `//`，写在对象上方单独一行，不要尾随在语句后
- 多行用 `/** ... */`，不要用多行 `//`
- 函数、类、文件、事件等使用文档类注释
